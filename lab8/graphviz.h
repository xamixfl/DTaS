#ifndef GRAPHVIZ_H
#define GRAPHVIZ_H

#include "graph.h"

/**
 * @brief Экспортирует граф в формат DOT и создает PNG изображение.
 *
 * Для варианта 2 (проверка связности неориентированного графа):
 * - Граф рисуется как неориентированный.
 * - Вершины разных компонент связности окрашиваются разными цветами.
 * - Если граф связный — все вершины одного цвета.
 *
 * @param g Указатель на граф.
 * @param filename Базовое имя файла (без расшинения).
 * @return Код ошибки (OK или код ошибки).
 */
int graph_export_to_dot(graph_t *g, const char *filename);

#endif // GRAPHVIZ_H
